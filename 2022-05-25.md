
# 2022-5/25

# 1,2교시 (c언어)

### void포인터
* 자료형이 정해지지 않음
* 다양한 자료형을 받을 수 있다.

**주의**
* 자료형을 받는다 하여도 void의 자료형이 바뀌는 것이 아님.

---
### void포인터의 다운 연산
```c
#include <stdio.h>
int main()
{
    int num = 10;
    int *numPtr1;
    int **numPtr2;//이중 포인터의 선언
    numPtr1=&num;
    numPtr2=&numPtr1;
    **numPtr2++
    printf("%d",**numPtr);//11출력
}
```
<b> **numPtr2</b>
<p>numPtr2 -> numPtr1 -> num</p>
up up 해서 num 까지 도달.

---git
